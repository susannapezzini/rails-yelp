<%= simple_form_for [ @restaurant, @review ] do |f| %>
  <%= f.label :content %>
  <%= f.text_area :content %>
  <%= f.input :rating %>
  <%= f.submit "Submit review", class: "btn btn-primary mt-3" %>
<% end %>